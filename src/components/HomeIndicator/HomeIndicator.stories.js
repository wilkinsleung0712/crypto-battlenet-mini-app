import { HomeIndicator } from ".";

export default {
  title: "Components/HomeIndicator",
  component: HomeIndicator,
};

export const Default = {
  args: {
    onOverlay: true,
    className: {},
  },
};
